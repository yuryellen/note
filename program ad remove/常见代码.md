** conda
- conda activate 名字
- conda create --name 名字
** python
- python -m venv 名字
- venv、 
- Windows myenv\Scripts\activate，
- Linux和macOS上 source myenv/bin/activate
- deactivate 退出虚拟环境

# 删除目录
- rm -rf .venv  # Linux/macOS
- del /s /q .venv  # Windows (命令提示符)
** 
打开虚拟环境快捷键 ctrl shift p